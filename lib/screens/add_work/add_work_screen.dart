import 'package:flutter/material.dart';
import 'components/body.dart';

class AddWorkScreen extends StatelessWidget {
  static String routeName = "/add-work";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
